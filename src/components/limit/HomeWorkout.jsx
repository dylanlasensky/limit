import React from 'react';
import {useNavigate} from 'react-router-dom';
import {Dumbbell,ArrowRight} from 'lucide-react';
export default function HomeWorkout({day,exerciseCount=0}){const nav=useNavigate();return <section className="rounded-2xl border border-zinc-800 bg-[#121217] p-5 text-white"><div className="flex items-center gap-2 text-blue-500"><Dumbbell className="h-4 w-4"/><span className="text-xs font-bold uppercase tracking-widest">Today’s workout</span></div><h2 className="mt-4 text-3xl font-black uppercase tracking-tight">{day?.name||'Recovery day'}</h2><p className="mt-1 text-sm text-zinc-400">{day?.targetMuscles?.join(' • ')||'Rest, walk, and prepare for your next session.'}</p>{day&&!day.isRest&&<><p className="mt-3 text-xs font-semibold text-zinc-500">{exerciseCount||6} EXERCISES · ~{Math.max(45,(exerciseCount||6)*9)} MIN</p><button onClick={()=>nav('/live-workout')} className="mt-5 flex h-12 w-full items-center justify-center gap-2 rounded-xl bg-blue-600 font-bold text-white transition-transform active:scale-[.98]">START WORKOUT <ArrowRight className="h-4 w-4"/></button></>}</section>}