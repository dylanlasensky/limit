import React from 'react';
export default function RatingHistory({snapshots}){if(snapshots.length<2)return null;const ordered=[...snapshots].reverse();const levels=ordered.map(x=>x.overallLevel).filter((x,i,a)=>i===0||x!==a[i-1]);return <div className="mt-5 border-t border-zinc-800 pt-4"><p className="text-xs font-bold tracking-[.16em] text-zinc-500">RATING HISTORY</p><div className="mt-3 flex flex-wrap items-center gap-2 text-xs font-bold">{levels.map((x,i)=><React.Fragment key={`${x}-${i}`}><span className="rounded-lg bg-zinc-900 px-2 py-1.5">{x.toUpperCase()}</span>{i<levels.length-1&&<span className="text-blue-500">→</span>}</React.Fragment>)}</div></div>}