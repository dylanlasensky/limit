import {base44} from '@/api/base44Client';
const schema={type:'object',properties:{kind:{type:'string',enum:['nutrition_label','package','meal','unknown']},title:{type:'string'},brand:{type:'string'},variant:{type:'string'},packageSize:{type:'string'},reliable:{type:'boolean'},servingSize:{type:'string'},servingsPerContainer:{type:'number'},message:{type:'string'},clarifyingQuestion:{type:'string'},possibleAllergens:{type:'array',items:{type:'string'}},nutrients:{type:'object',properties:{calories:{type:'number'},protein:{type:'number'},carbs:{type:'number'},fat:{type:'number'},fiber:{type:'number'},sugar:{type:'number'},sodium:{type:'number'}}},items:{type:'array',items:{type:'object',properties:{name:{type:'string'},amount:{type:'number'},unit:{type:'string'},calories:{type:'number'},protein:{type:'number'},carbs:{type:'number'},fat:{type:'number'},fiber:{type:'number'},sugar:{type:'number'},sodium:{type:'number'}}}}},required:['kind','title','reliable','possibleAllergens','nutrients','items']};
export async function uploadFoodImage(file){return (await base44.integrations.Core.UploadFile({file})).file_url}
export async function analyzeFoodImage({fileUrl,scanMode,dietaryProfile,clarification}){const meal=scanMode==='meal';return base44.integrations.Core.InvokeLLM({file_urls:[fileUrl],response_json_schema:schema,prompt:`Analyze this food image for the Limit nutrition tracker. Requested mode: ${meal?'actual plated meal':'packaged food or Nutrition Facts label'}. First classify it as nutrition_label, package, meal, or unknown. ${meal?'Identify each visible food, estimate its US customary portion (oz, cups, tbsp, tsp, serving, or piece), estimate calories/protein/carbs/fat for each item, and sum nothing outside the items. Estimates are allowed but must be conservative. Ask at most ONE short clarifyingQuestion only if the answer would materially change the estimate.':'If a readable Nutrition Facts label is visible, transcribe per-serving values exactly and set reliable true. Extract serving size, servings per container, calories, protein, total carbs, fat, fiber, sugar, and sodium. If only the package front is visible, identify brand/product/variant/size but set reliable false and leave unknown nutrient numbers as 0; do not invent exact nutrition and advise scanning the label.'} Flag possible allergens visible or reasonably suggested by the food, without claiming certainty. Saved allergies: ${JSON.stringify(dietaryProfile?.allergies||[])}. ${clarification?`The user answered your one clarification: ${clarification}. Return a revised final analysis and no further question.`:''}`})}
export function conflictsFor(result,dietaryProfile){const saved=(dietaryProfile?.allergies||[]).map(x=>x.toLowerCase());return(result.possibleAllergens||[]).filter(x=>saved.some(a=>x.toLowerCase().includes(a)||a.includes(x.toLowerCase())))}