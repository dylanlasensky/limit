{
  "name": "WeeklyMealPlan",
  "type": "object",
  "properties": {
    "weekStart": {
      "type": "string",
      "format": "date"
    },
    "mealIds": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "calorieTotalsByDay": {
      "type": "object"
    },
    "macroTotalsByDay": {
      "type": "object"
    }
  },
  "required": [
    "weekStart"
  ],
  "rls": {
    "read": {
      "created_by_id": "{{user.id}}"
    },
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "update": {
      "created_by_id": "{{user.id}}"
    },
    "delete": {
      "created_by_id": "{{user.id}}"
    }
  }
}