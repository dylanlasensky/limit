import React,{useMemo,useState} from 'react';
import {Plus,Trash2} from 'lucide-react';
import {base44} from '@/api/base44Client';
import {today,sumMacros} from '@/components/limit/data';
const macroKeys=['calories','protein','carbs','fat'];
const nutrientKeys=[...macroKeys,'fiber','sugar','sodium'];
export default function ScannedMealEditor({result,conflicts,onDone}){const[items,setItems]=useState(result.items||[]),[mealType,setMealType]=useState('Dinner'),[saving,setSaving]=useState(false);const totals=useMemo(()=>sumMacros(items),[items]);const edit=(i,k,v)=>setItems(a=>a.map((x,j)=>{if(j!==i)return x;if(k==='amount'){const next=+v,ratio=(x.amount&&next)?next/x.amount:1;return{...x,amount:next,...Object.fromEntries(macroKeys.map(m=>[m,Math.round((x[m]||0)*ratio*10)/10]))}}return{...x,[k]:k==='name'||k==='unit'?v:+v}}));const remove=i=>setItems(a=>a.filter((_,j)=>j!==i));const add=()=>setItems(a=>[...a,{name:'',amount:1,unit:'serving',calories:0,protein:0,carbs:0,fat:0}]);const save=async()=>{setSaving(true);await base44.entities.FoodEntry.create({date:today(),mealType,foodName:items.map(x=>x.name).filter(Boolean).join(', ')||'Scanned meal',quantity:1,unit:'meal',...Object.fromEntries(nutrientKeys.map(k=>[k,items.reduce((sum,x)=>sum+(+x[k]||0),0)])),entryMethod:'scan_meal',estimated:true});onDone()};return <div className="space-y-4"><div><p className="text-xs font-bold text-blue-500">AI MEAL ESTIMATE</p><p className="mt-1 text-xs text-zinc-400">Estimated nutrition — adjust any food or portion before logging.</p></div>{conflicts.length>0&&<div className="rounded-xl border border-red-500/60 bg-red-950/40 p-3"><b className="text-red-300">Possible allergy conflict</b><p className="mt-1 text-xs text-red-200">This meal may contain {conflicts.join(', ')}. Verify ingredients before eating; visual AI cannot confirm allergens.</p></div>}{items.map((x,i)=><div key={i} className="rounded-xl border border-zinc-800 bg-zinc-950 p-3"><div className="flex gap-2"><input value={x.name} onChange={e=>edit(i,'name',e.target.value)} className="h-10 min-w-0 flex-1 bg-transparent font-bold"/><button onClick={()=>remove(i)}><Trash2 className="h-4 w-4 text-zinc-500"/></button></div><div className="grid grid-cols-2 gap-2"><input type="number" value={x.amount||''} onChange={e=>edit(i,'amount',e.target.value)} className="h-10 rounded-lg border border-zinc-700 bg-transparent px-2"/><input value={x.unit||''} onChange={e=>edit(i,'unit',e.target.value)} className="h-10 rounded-lg border border-zinc-700 bg-transparent px-2"/></div><div className="mt-2 grid grid-cols-4 gap-1">{macroKeys.map(k=><label key={k} className="text-[10px] uppercase text-zinc-500">{k==='calories'?'cal':k}<input type="number" value={x[k]||0} onChange={e=>edit(i,k,e.target.value)} className="mt-1 h-9 w-full rounded-lg border border-zinc-800 bg-transparent px-1 text-center text-xs text-white"/></label>)}</div></div>)}<button onClick={add} className="flex items-center gap-2 text-sm font-bold text-blue-500"><Plus className="h-4 w-4"/>Add missing food</button><div className="rounded-xl border border-zinc-800 p-4"><p className="text-xs text-zinc-500">ESTIMATED TOTAL</p><b className="text-2xl">{Math.round(totals.calories)} calories</b><p className="mt-1 text-sm text-zinc-400">{Math.round(totals.protein)}g protein · {Math.round(totals.carbs)}g carbs · {Math.round(totals.fat)}g fat</p></div><select value={mealType} onChange={e=>setMealType(e.target.value)} className="h-12 w-full rounded-xl border border-zinc-700 bg-zinc-900 px-3">{[['Breakfast','Breakfast'],['Lunch','Lunch'],['Dinner','Dinner'],['Snacks','Snack']].map(([v,l])=><option key={v} value={v}>{l}</option>)}</select><button disabled={saving||!items.length} onClick={save} className="h-12 w-full rounded-xl bg-blue-600 font-bold text-white">{saving?'LOGGING…':'LOG MEAL'}</button></div>}