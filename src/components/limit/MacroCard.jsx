import React from 'react';
export default function MacroCard({label,value=0,goal=0,unit='g',color='bg-blue-600'}){const pct=Math.min(100,Math.round(value/Math.max(goal,1)*100));return <div className="min-w-0 rounded-2xl border border-border bg-card p-3 sm:p-4"><div className="flex flex-wrap items-end justify-between gap-1"><b className="text-xs sm:text-sm">{label}</b><span className="text-[10px] text-muted-foreground">{Math.max(0,Math.round(goal-value))}{unit} left</span></div><p className="mt-2 truncate text-lg font-bold tabular-nums sm:text-xl">{Math.round(value)} <span className="text-xs font-medium text-muted-foreground">/ {goal}{unit}</span></p><div className="mt-3 h-1.5 overflow-hidden rounded-full bg-secondary"><div className={`h-full rounded-full transition-all duration-500 ${color}`} style={{width:`${pct}%`}}/></div></div>}