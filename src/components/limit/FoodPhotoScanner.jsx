import React,{useState} from 'react';import {Camera,ImagePlus,Loader2,RotateCcw} from 'lucide-react';import {Image} from '@/components/ui/image';import {analyzeFoodImage,conflictsFor,uploadFoodImage} from '@/components/limit/foodImageAnalysis';import ScannedFoodEditor from '@/components/limit/ScannedFoodEditor';import ScannedMealEditor from '@/components/limit/ScannedMealEditor';
export default function FoodPhotoScanner({mode,dietaryProfile,onDone}){const[url,setUrl]=useState(''),[result,setResult]=useState(),[loading,setLoading]=useState(false),[error,setError]=useState(''),[answer,setAnswer]=useState('');const analyze=async(fileUrl,clarification)=>{setLoading(true);setError('');try{const next=await analyzeFoodImage({fileUrl,scanMode:mode,dietaryProfile,clarification});if(clarification)next.clarifyingQuestion='';setResult(next)}catch(e){setError(e.message||'We could not analyze this photo.')}finally{setLoading(false)}};const choose=async e=>{const file=e.target.files?.[0];if(!file)return;setLoading(true);try{const fileUrl=await uploadFoodImage(file);setUrl(fileUrl);await analyze(fileUrl)}catch(err){setError(err.message||'Upload failed.');setLoading(false)}};const reset=()=>{setUrl('');setResult();setError('')};if(result?.clarifyingQuestion&&mode==='meal')return <div className="space-y-3"><p className="font-bold">One quick question</p><p className="text-sm text-zinc-300">{result.clarifyingQuestion}</p><input value={answer} onChange={e=>setAnswer(e.target.value)} className="h-12 w-full rounded-xl border border-zinc-700 bg-transparent px-3"/><button onClick={()=>analyze(url,answer)} disabled={!answer||loading} className="h-12 w-full rounded-xl bg-blue-600 font-bold">Refine estimate</button><button onClick={()=>setResult({...result,clarifyingQuestion:''})} className="h-10 w-full text-sm text-zinc-400">Use current estimate</button></div>;if(result&&mode==='food'&&!result.reliable)return <div className="space-y-4"><p className="text-xs font-bold text-blue-500">NUTRITION NOT VERIFIED</p><h3 className="text-xl font-black">{[result.brand,result.title,result.variant].filter(Boolean).join(' ')}</h3><p className="text-sm text-zinc-400">{result.message||"We couldn't verify the nutrition information. Scan the Nutrition Facts label for a more accurate result."}</p><button onClick={reset} className="flex h-12 w-full items-center justify-center gap-2 rounded-xl bg-blue-600 font-bold"><RotateCcw className="h-4 w-4"/>SCAN NUTRITION LABEL</button></div>;if(result)return mode==='meal'?<ScannedMealEditor result={result} conflicts={conflictsFor(result,dietaryProfile)} onDone={onDone}/>:<ScannedFoodEditor result={result} onDone={onDone}/>;return <div className="space-y-4">{url&&<Image src={url} alt="Selected food" className="h-48 w-full rounded-2xl" fittingType="fill"/>}{loading?<div className="grid min-h-40 place-items-center text-center"><div><Loader2 className="mx-auto h-7 w-7 animate-spin text-blue-500"/><p className="mt-3 text-sm">Analyzing photo…</p></div></div>:<><p className="text-sm text-zinc-400">{mode==='meal'?'Photograph the full plate in good lighting.':'Photograph a Nutrition Facts label or the front of the package.'}</p><div className="grid grid-cols-2 gap-3"><label className="grid h-24 cursor-pointer place-items-center rounded-xl border border-zinc-700 bg-zinc-950 text-sm font-bold"><span className="text-center"><Camera className="mx-auto mb-2"/>Camera</span><input type="file" accept="image/*" capture="environment" onChange={choose} className="hidden"/></label><label className="grid h-24 cursor-pointer place-items-center rounded-xl border border-zinc-700 bg-zinc-950 text-sm font-bold"><span className="text-center"><ImagePlus className="mx-auto mb-2"/>Photo Library</span><input type="file" accept="image/*" onChange={choose} className="hidden"/></label></div></>}{error&&<p className="rounded-xl bg-red-950/40 p-3 text-sm text-red-300">{error}</p>}</div>}