import React from 'react';
import {Link} from 'react-router-dom';
const Metric=({to,label,value,detail,emphasis})=><Link to={to} className="min-w-0 px-3 py-3 first:pl-0 last:pr-0"><p className="text-[9px] font-bold uppercase tracking-wider text-muted-foreground">{label}</p><p className={`mt-1 truncate text-lg font-extrabold tabular-nums ${emphasis?'text-primary':'text-foreground'}`}>{value}</p><p className="truncate text-[10px] text-muted-foreground">{detail}</p></Link>;
export default function HomeSnapshot({calories,calorieGoal,protein,proteinGoal,weekDone,weekGoal,weight}){return <section className="mt-4 border-y border-border/70"><div className="grid grid-cols-4 divide-x divide-border/60"><Metric to="/nutrition" label="Calories" value={calorieGoal?Math.max(0,calorieGoal-calories).toLocaleString():'—'} detail="left"/><Metric to="/nutrition" label="Protein" value={proteinGoal?`${Math.max(0,proteinGoal-protein)}g`:'—'} detail="left" emphasis/><Metric to="/workout" label="Week" value={`${weekDone}/${weekGoal||'—'}`} detail="sessions"/><Metric to="/progress" label="Weight" value={weight?`${weight}`:'—'} detail="lb"/></div></section>}