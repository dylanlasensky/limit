import React from 'react';
const allergens=['Eggs','Peanuts','Tree nuts','Shellfish','Fish','Milk/dairy','Soy','Wheat/gluten','Sesame'];const prefs=['High protein','Vegetarian','Vegan','Pescatarian','Dairy free','Gluten free'];
export default function OnboardingDiet({data,set}){const toggle=(key,x)=>set(key,(data[key]||[]).includes(x)?data[key].filter(v=>v!==x):[...(data[key]||[]),x]);return <div className="space-y-5"><div><p className="font-bold">Allergies</p><div className="mt-2 flex flex-wrap gap-2">{allergens.map(x=><button onClick={()=>toggle('allergies',x)} className={`rounded-full border px-3 py-2 text-sm ${(data.allergies||[]).includes(x)?'border-red-500 bg-red-50 text-red-700':''}`}>{x}</button>)}</div></div><div><p className="font-bold">Preferences</p><div className="mt-2 flex flex-wrap gap-2">{prefs.map(x=><button onClick={()=>toggle('dietaryPreferences',x)} className={`rounded-full border px-3 py-2 text-sm ${(data.dietaryPreferences||[]).includes(x)?'border-blue-500 bg-blue-50 text-blue-800':''}`}>{x}</button>)}</div></div><input className="h-12 w-full rounded-xl border bg-transparent px-3" placeholder="Other foods to avoid, comma separated" value={data.avoidText||''} onChange={e=>set('avoidText',e.target.value)}/><p className="rounded-xl bg-amber-50 p-3 text-xs text-amber-900">Limit excludes saved allergens from meal ideas. Always verify labels and cross-contact warnings for serious allergies.</p></div>}