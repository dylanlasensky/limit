import React,{useState} from 'react';import {Camera,Search,Utensils,Store,PenLine,ChevronLeft} from 'lucide-react';import ManualFood from '@/components/limit/ManualFood';import FoodPhotoScanner from '@/components/limit/FoodPhotoScanner';
const options=[['Search Food',Search,'search'],['Scan Food',Camera,'food'],['Scan Meal',Utensils,'meal'],['Restaurant Meal',Store,'restaurant'],['Manual Entry',PenLine,'manual']];
export default function AddFoodFlow({dietaryProfile,onDone}){const[mode,setMode]=useState();if(mode)return <div><button onClick={()=>setMode()} className="mb-4 flex items-center gap-1 text-sm text-zinc-400"><ChevronLeft className="h-4 w-4"/>All options</button><h3 className="mb-4 text-lg font-black">{options.find(x=>x[2]===mode)?.[0]}</h3>{['food','meal'].includes(mode)?<FoodPhotoScanner mode={mode} dietaryProfile={dietaryProfile} onDone={onDone}/>:<ManualFood entryMethod={mode} estimated={mode==='restaurant'} onDone={onDone}/>}</div>;return <div className="grid gap-2">{options.map(([label,Icon,value])=><button onClick={()=>setMode(value)} className="flex h-14 items-center gap-3 rounded-xl border border-zinc-800 bg-zinc-950 px-4 text-left font-bold"><Icon className="h-5 w-5 text-blue-500"/>{label}</button>)}</div>}