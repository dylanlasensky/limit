import React from 'react';
export default function MacroCard({label,value=0,goal=0,unit='g',color='bg-primary'}){
  const pct=Math.min(100,Math.round(value/Math.max(goal,1)*100));
  return <div className="limit-surface relative min-w-0 overflow-hidden rounded-2xl p-4"><div className="absolute inset-x-0 bottom-0 h-16 bg-gradient-to-t from-primary/5 to-transparent"/><div className="relative flex items-start justify-between gap-2"><div><p className="text-[9px] font-black uppercase tracking-[.18em] text-muted-foreground">{label}</p><p className="mt-2 text-2xl font-black tabular-nums">{Math.round(value)}<span className="ml-1 text-xs font-medium text-muted-foreground">{unit}</span></p></div><span className="text-[10px] tabular-nums text-muted-foreground">{Math.max(0,Math.round(goal-value))}{unit}<br/>left</span></div><div className="relative mt-4 h-1 overflow-hidden rounded-full bg-secondary"><div className={`h-full rounded-full shadow-[0_0_12px_hsl(var(--primary)/.6)] transition-all duration-700 ${color}`} style={{width:`${pct}%`}}/></div></div>;
}