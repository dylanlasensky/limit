import React,{useState} from 'react';
export default function MealPrepCard({meal,onAdd}){const[servings,setServings]=useState(5);if(!meal)return null;return <section className="mb-4 rounded-2xl bg-zinc-950 p-5 text-white dark:bg-zinc-900"><p className="text-xs font-bold uppercase tracking-wide text-blue-400">Meal prep mode</p><h3 className="mt-2 text-xl font-black">{meal.name}</h3><div className="mt-3 flex items-center justify-between"><span className="text-sm text-zinc-400">Batch servings</span><div className="flex items-center gap-3"><button onClick={()=>setServings(Math.max(2,servings-1))} className="h-9 w-9 rounded-lg bg-zinc-800">−</button><b>{servings}</b><button onClick={()=>setServings(servings+1)} className="h-9 w-9 rounded-lg bg-zinc-800">+</button></div></div><p className="mt-3 text-sm">Per serving · {meal.calories} cal · {meal.protein}g protein</p><button onClick={()=>onAdd({...meal,servings})} className="mt-4 h-11 w-full rounded-xl bg-blue-600 font-bold text-white">Use for this week</button></section>}