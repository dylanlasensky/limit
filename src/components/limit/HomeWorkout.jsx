import React from 'react';import {useNavigate} from 'react-router-dom';import {Dumbbell,ArrowRight} from 'lucide-react';
export default function HomeWorkout({day}){const nav=useNavigate();return <section className="rounded-[1.75rem] bg-zinc-950 p-5 text-white dark:bg-zinc-900"><div className="flex items-center gap-2 text-lime-300"><Dumbbell className="h-4 w-4"/><span className="text-xs font-bold uppercase tracking-widest">Today’s workout</span></div><h2 className="mt-4 text-2xl font-black">{day?.name||'Recovery day'}</h2><p className="mt-1 text-sm text-zinc-400">{day?.targetMuscles?.join(' • ')||'Rest, walk, and prepare for your next session.'}</p>{day&&!day.isRest&&<button onClick={()=>nav('/live-workout')} className="mt-5 flex h-12 w-full items-center justify-center gap-2 rounded-xl bg-lime-400 font-bold text-zinc-950">Start workout <ArrowRight className="h-4 w-4"/></button>}</section>}