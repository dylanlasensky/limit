import React from 'react';
export default function MacroCard({label,value,goal,unit='g',color='bg-blue-600'}){const pct=Math.min(100,Math.round(value/Math.max(goal,1)*100));return <div className="rounded-2xl border border-zinc-800 bg-[#121217] p-4"><div className="flex items-end justify-between"><b className="text-sm">{label}</b><span className="text-xs text-zinc-500">{Math.max(0,goal-value)}{unit} left</span></div><p className="mt-2 text-xl font-bold">{value} <span className="text-sm font-medium text-zinc-400">/ {goal}{unit}</span></p><div className="mt-3 h-2 overflow-hidden rounded-full bg-zinc-100 dark:bg-zinc-800"><div className={`h-full rounded-full ${color}`} style={{width:`${pct}%`}}/></div></div>}