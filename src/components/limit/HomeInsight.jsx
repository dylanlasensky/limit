import React from 'react';
import {ArrowUpRight} from 'lucide-react';
import {Link} from 'react-router-dom';
export default function HomeInsight({records=[],weekDone=0,weekGoal=0,proteinShort=0}){let text='Complete a workout to start building your training signal.',to='/workout';if(records.length){const record=records[0];text=`Latest PR: ${record.exerciseName} · ${Math.round(record.value)} lb`;to='/progress'}else if(weekDone){text=`${weekDone} of ${weekGoal||weekDone} workouts complete this week.`}else if(proteinShort>0){text=`Protein is ${proteinShort}g short today.`;to='/nutrition'}return <Link to={to} className="group mt-5 flex items-center justify-between gap-4 border-l-2 border-primary py-1 pl-4"><div><p className="text-[9px] font-bold uppercase tracking-wider text-muted-foreground">Today’s signal</p><p className="mt-1 text-sm font-semibold leading-snug">{text}</p></div><ArrowUpRight className="h-4 w-4 shrink-0 text-primary transition-transform group-active:translate-x-0.5"/></Link>}