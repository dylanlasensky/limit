import React,{useEffect,useState} from 'react';import {base44} from '@/api/base44Client';import {Plus,X} from 'lucide-react';import {today,sumMacros} from '@/components/limit/data';import MacroCard from '@/components/limit/MacroCard';import MealPlanner from '@/components/limit/MealPlanner';import AddFoodFlow from '@/components/limit/AddFoodFlow';import DailyTargetSummary from '@/components/limit/DailyTargetSummary';
export default function Nutrition(){const[tab,setTab]=useState('Today'),[foods,setFoods]=useState([]),[p,setP]=useState({}),[diet,setDiet]=useState({}),[add,setAdd]=useState(false);const load=()=>Promise.all([base44.entities.FoodEntry.filter({date:today()}),base44.entities.UserProfile.list(),base44.entities.DietaryProfile.list()]).then(([f,a,d])=>{setFoods(f);setP(a[0]||{});setDiet(d[0]||{})});useEffect(()=>{load()},[]);const m=sumMacros(foods);return <div><header><p className="text-sm text-zinc-500">Fuel with intent</p><h1 className="text-3xl font-black">Nutrition</h1></header><div className="my-5 grid grid-cols-3 rounded-xl bg-zinc-800 p-1">{['Today','Meal Ideas','Meal Plan'].map(x=><button key={x} onClick={()=>setTab(x)} className={`h-10 rounded-lg text-sm font-bold ${tab===x?'bg-zinc-700 shadow-sm':''}`}>{x}</button>)}</div>{tab==='Today'?<><DailyTargetSummary profile={p}/><div className="grid grid-cols-2 gap-3"><MacroCard label="Calories" value={m.calories} goal={p.calorieTarget||2200} unit=""/><MacroCard label="Protein" value={m.protein} goal={p.proteinTarget||150}/><MacroCard label="Carbs" value={m.carbs} goal={p.carbTarget||250}/><MacroCard label="Fat" value={m.fat} goal={p.fatTarget||70}/></div>{['Breakfast','Lunch','Dinner','Snacks'].map(type=><section key={type} className="mt-5"><h2 className="font-bold">{type==='Snacks'?'Snack':type}</h2>{foods.filter(x=>x.mealType===type).map(x=><div key={x.id} className="mt-2 flex justify-between rounded-xl border border-zinc-800 bg-[#121217] p-3 text-sm"><span>{x.foodName}</span><b>{x.calories||0} cal</b></div>)}</section>)}<button onClick={()=>setAdd(true)} className="mt-6 flex h-14 w-full items-center justify-center gap-2 rounded-2xl bg-blue-600 font-bold text-white"><Plus/>ADD FOOD</button></>:<MealPlanner mode={tab} profile={p} onLogged={load}/>} {add&&<div className="fixed inset-0 z-50 flex items-end bg-black/60"><div className="mx-auto max-h-[92vh] w-full max-w-md overflow-y-auto rounded-t-[2rem] border-t border-zinc-800 bg-[#121217] p-5 pb-[max(1.25rem,env(safe-area-inset-bottom))]"><button onClick={()=>setAdd(false)} className="float-right p-2"><X/></button><h2 className="mb-5 text-xl font-black">Add food</h2><AddFoodFlow dietaryProfile={diet} onDone={()=>{setAdd(false);load()}}/></div></div>}</div>}