import React, { useState } from 'react';
import { LineChart,Line,XAxis,YAxis,ResponsiveContainer,Tooltip } from 'recharts';
export default function WeightProgress({ weights, current, average, goal, onLog, saving }) {const[value,setValue]=useState('');const submit=()=>{if(+value>0){onLog(+value);setValue('')}};return <><section className="rounded-3xl border border-border bg-card p-5"><p className="text-[10px] font-bold uppercase tracking-widest text-muted-foreground">Current weight</p><p className="mt-2 text-4xl font-black tabular-nums">{current||'—'} <span className="text-sm text-muted-foreground">lb</span></p><p className="mt-2 text-sm text-primary">7-day average {average?average.toFixed(1):'—'}{goal?` · Goal ${goal} lb`:''}</p></section>{weights.length>=2?<section className="mt-4 h-56 rounded-3xl border border-border bg-card p-3"><ResponsiveContainer width="100%" height="100%"><LineChart data={weights}><XAxis dataKey="date" hide/><YAxis domain={['dataMin - 2','dataMax + 2']} hide/><Tooltip contentStyle={{background:'hsl(var(--card))',border:'1px solid hsl(var(--border))',borderRadius:12}}/><Line type="monotone" dataKey="weight" stroke="#2563EB" strokeWidth={3} dot={false}/></LineChart></ResponsiveContainer></section>:<section className="mt-4 rounded-2xl border border-dashed border-border p-5 text-sm text-muted-foreground">Log at least two weigh-ins to reveal your trend. Daily fluctuations are normal.</section>}<div className="mt-4 flex gap-2"><input type="number" inputMode="decimal" value={value} onChange={e=>setValue(e.target.value)} placeholder="Today’s weight" className="h-12 min-w-0 flex-1 rounded-xl border border-border bg-card px-3"/><button disabled={!value||saving} onClick={submit} className="rounded-xl bg-primary px-5 font-bold text-primary-foreground disabled:opacity-40">LOG</button></div></>}