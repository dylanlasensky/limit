import React,{useEffect} from 'react';
import {useLocation,useNavigate} from 'react-router-dom';
import {House,Dumbbell,Apple,TrendingUp,UserRound} from 'lucide-react';
const items=[['Home','/home',House],['Workout','/workout',Dumbbell],['Nutrition','/nutrition',Apple],['Progress','/progress',TrendingUp],['Profile','/profile',UserRound]];
const isWithin=(pathname,root)=>pathname===root||pathname.startsWith(`${root}/`);
export default function BottomNav(){const location=useLocation(),navigate=useNavigate();useEffect(()=>{const item=items.find(([,root])=>isWithin(location.pathname,root));if(item)sessionStorage.setItem(`limit-tab-route:${item[1]}`,`${location.pathname}${location.search}${location.hash}`)},[location]);const select=root=>{const active=isWithin(location.pathname,root);if(active){window.scrollTo({top:0,behavior:'smooth'});navigate(root,{replace:true});sessionStorage.setItem(`limit-tab-route:${root}`,root);return}navigate(sessionStorage.getItem(`limit-tab-route:${root}`)||root)};return <nav className="fixed inset-x-0 bottom-0 z-40 mx-auto max-w-md border-t border-border bg-background/95 px-2 pb-[max(.45rem,env(safe-area-inset-bottom))] pt-2 backdrop-blur-xl"><div className="flex justify-around">{items.map(([label,root,Icon])=>{const active=isWithin(location.pathname,root);return <button key={root} onClick={()=>select(root)} aria-current={active?'page':undefined} className={`flex min-h-12 min-w-14 flex-col items-center justify-center gap-1 rounded-xl text-[10px] font-semibold transition-colors ${active?'text-blue-500':'text-zinc-500'}`}><Icon className="h-5 w-5"/>{label}</button>})}</div></nav>}