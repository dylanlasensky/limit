import React,{useRef,useState} from 'react';import {RefreshCw} from 'lucide-react';
export default function PullToRefresh({onRefresh,children}){const start=useRef(null),[pull,setPull]=useState(0),[refreshing,setRefreshing]=useState(false);const begin=e=>{if(window.scrollY<=0)start.current=e.touches[0].clientY};const move=e=>{if(start.current===null)return;setPull(Math.min(72,Math.max(0,(e.touches[0].clientY-start.current)*.35)))};const end=async()=>{start.current=null;if(pull>=48){setRefreshing(true);await onRefresh();setRefreshing(false)}setPull(0)};return <div onTouchStart={begin} onTouchMove={move} onTouchEnd={end} className="relative"><div className="pointer-events-none flex items-center justify-center overflow-hidden text-blue-500 transition-[height]" style={{height:pull}}><RefreshCw className={`h-5 w-5 ${refreshing?'animate-spin':''}`}/></div>{children}</div>}